memory management
 - free memory when exiting due to error
 - cut down on exit() calls

impl
 - `=` command should set dot
 - `r`/`w` output byte counts
 - `r`/`w` to/from a program (r !ls)

testing
 - unit tests for everything?

documentation
 - improve manual page
